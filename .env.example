# Copy to ``api/.env`` for local FastAPI + Alembic.
DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:5432/eventsee

# Optional: comma-separated browser origins for credentialed CORS.
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
